// Problems live in NeetCode's folder layout (<problem-id>/submission-N.rs) so this
// folder reads the same as the Python one next door. #[path] points Cargo at them.

#[path = "../matrix-depth-first-search/submission-0.rs"]
pub mod matrix_depth_first_search;
